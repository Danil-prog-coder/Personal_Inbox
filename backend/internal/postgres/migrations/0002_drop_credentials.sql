-- Вход и регистрация убраны: приложение локальное, пользователь один и он же
-- владелец машины. Учётных данных больше нет — ни адреса почты, ни пароля
-- (решение №50).
--
-- Таблица users остаётся: к ней привязаны подключения и сообщения, и строка
-- в ней хранит настройки — критерии важности, тему, плотность. Просто теперь
-- строка ровно одна.
ALTER TABLE users DROP COLUMN IF EXISTS email;
ALTER TABLE users DROP COLUMN IF EXISTS password_hash;
